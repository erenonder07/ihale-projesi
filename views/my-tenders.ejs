<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>İlanlarım ve Teklifler</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f8f9fa; }
        .tender-card {
            border: none;
            border-radius: 12px;
            overflow: hidden;
            transition: transform 0.2s;
        }
        .tender-card:hover { transform: translateY(-3px); }
        .table-hover tbody tr:hover { background-color: #f1f3f5; }
        .user-avatar {
            width: 32px; height: 32px;
            background-color: #e9ecef; color: #495057;
            border-radius: 50%; display: inline-flex;
            align-items: center; justify-content: center;
            font-size: 14px; margin-right: 8px;
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-dark bg-dark mb-4 shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/dashboard">
                 🔥 İhale<span class="text-primary">Paneli</span>
            </a>
            <a href="/dashboard" class="btn btn-sm btn-outline-light">
                <i class="fas fa-arrow-left"></i> Panele Dön
            </a>
        </div>
    </nav>

    <div class="container pb-5">
        <div class="d-flex align-items-center justify-content-between mb-4">
            <h3 class="fw-bold text-secondary">📜 İlanlarım ve Gelen Teklifler</h3>
            <span class="badge bg-primary fs-6"><%= tenders.length %> İlan</span>
        </div>

        <% if (tenders.length === 0) { %>
            <div class="alert alert-info py-4 text-center shadow-sm border-0">
                <i class="fas fa-folder-open fa-2x mb-3 text-info"></i><br>
                Henüz açtığınız bir ihale bulunmuyor.<br>
                <a href="/yeni-ilan" class="fw-bold mt-2 d-inline-block">Hemen bir tane oluşturun!</a>
            </div>
        <% } %>

        <div class="row">
            <% tenders.forEach(tender => { %>
                
                <% 
                   const now = new Date();
                   const endDate = new Date(tender.end_date);
                   const isExpired = endDate < now; // Süre doldu mu?
                   
                   // Eğer süre dolduysa veya status 0 ise Pasif göster
                   const isActive = tender.status && !isExpired;
                %>

                <div class="col-12 mb-5">
                    <div class="card tender-card shadow-sm border-start border-5 <%= isActive ? 'border-success' : 'border-secondary' %>">
                        
                        <div class="card-header bg-white p-3 border-bottom d-flex align-items-center justify-content-between">
                            <div class="d-flex align-items-center gap-3">
                                <% if(tender.image_url) { %>
                                    <img src="/static/images/<%= tender.image_url %>" class="rounded" width="60" height="60" style="object-fit: cover;">
                                <% } else { %>
                                    <div class="bg-light rounded d-flex align-items-center justify-content-center" style="width: 60px; height: 60px;">
                                        <i class="fas fa-image text-muted"></i>
                                    </div>
                                <% } %>
                                <div>
                                    <h5 class="mb-0 fw-bold text-dark"><%= tender.title %></h5>
                                    <small class="text-muted d-block">Başlangıç: <%= tender.start_price %> ₺</small>
                                    
                                    <small class="<%= isExpired ? 'text-danger fw-bold' : 'text-success' %>" style="font-size: 11px;">
                                        <i class="fas fa-clock"></i> 
                                        <%= endDate.toLocaleString('tr-TR', { day: 'numeric', month: 'long', hour: '2-digit', minute: '2-digit' }) %>
                                    </small>
                                </div>
                            </div>
                            
                            <div>
                                <% if (isActive) { %>
                                    <span class="badge bg-success">
                                        <i class="fas fa-check-circle"></i> Yayında
                                    </span>
                                <% } else { %>
                                    <span class="badge bg-secondary">
                                        <i class="fas fa-lock"></i> Süre Doldu
                                    </span>
                                <% } %>
                            </div>
                        </div>

                        <div class="card-body p-0">
                            <% if (tender.Bids && tender.Bids.length > 0) { %>
                                <div class="table-responsive">
                                    <table class="table table-hover mb-0 align-middle">
                                        <thead class="table-light text-secondary small">
                                            <tr>
                                                <th class="ps-4">Teklif Veren</th>
                                                <th>İletişim</th>
                                                <th class="text-end pe-4">Tutar</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% tender.Bids.sort((a,b) => b.amount - a.amount).forEach((bid, index) => { %>
                                                <tr class="<%= index === 0 && isExpired ? 'table-success' : '' %>"> 
                                                    <td class="ps-4">
                                                        <div class="d-flex align-items-center">
                                                            <div class="user-avatar bg-primary text-white">
                                                                <i class="fas fa-user"></i>
                                                            </div>
                                                            <div>
                                                                <span class="fw-bold text-dark d-block">
                                                                    <%= bid.User ? bid.User.full_name : 'Bilinmiyor' %>
                                                                </span>
                                                                <% if(index === 0 && isExpired) { %>
                                                                    <span class="badge bg-warning text-dark" style="font-size: 10px;">🏆 KAZANAN</span>
                                                                <% } %>
                                                            </div>
                                                        </div>
                                                    </td>
                                                    <td class="small text-muted">
                                                        <%= bid.User ? bid.User.email : '-' %> <br>
                                                        <%= bid.User ? bid.User.phone : '' %>
                                                    </td>
                                                    <td class="text-end pe-4">
                                                        <span class="badge bg-success bg-opacity-10 text-success px-3 py-2 fs-6">
                                                            <%= new Intl.NumberFormat('tr-TR', { minimumFractionDigits: 2 }).format(bid.amount) %> ₺
                                                        </span>
                                                    </td>
                                                </tr>
                                            <% }) %>
                                        </tbody>
                                    </table>
                                </div>
                            <% } else { %>
                                <div class="text-center py-4 text-muted small">
                                    <i class="fas fa-wind mb-2 fa-2x opacity-50"></i><br>
                                    Henüz teklif gelmemiş.
                                </div>
                            <% } %>
                        </div>
                        
                        <div class="card-footer bg-light text-muted small d-flex justify-content-between">
                            <span>Oluşturulma: <%= new Date(tender.createdAt).toLocaleDateString('tr-TR') %></span>
                            <span>Toplam <%= tender.Bids ? tender.Bids.length : 0 %> Teklif</span>
                        </div>

                    </div>
                </div>
            <% }) %>
        </div>
    </div>

</body>
</html>