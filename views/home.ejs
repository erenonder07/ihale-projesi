<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>İhale Paneli</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <nav class="navbar navbar-dark bg-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">İhale Paneli</a>
            <a href="/login" class="btn btn-outline-light btn-sm">Çıkış / Kayıt</a>
        </div>
    </nav>

    <div class="container">
        

        <h3 class="mb-3">Aktif İhaleler</h3>
        
        <div class="row">
    <% if (ihaleler.length === 0) { %>
        <div class="alert alert-warning">Şu an açık bir ihale yok.</div>
    <% } %>

    <% ihaleler.forEach(ihale => { %>
        <div class="col-md-4 mb-4">
            <div class="card h-100 border-0 shadow-sm">
                <div class="card-body">
                    <h5 class="card-title text-primary"><%= ihale.title %></h5>
    <% if (ihale.image_url) { %>
        <img src="/static/images/<%= ihale.image_url %>" class="card-img-top" alt="Ürün Resmi" style="height: 200px; object-fit: cover;">
    <% } %>

        <div class="card-body">
        <h5 class="card-title"><%= ihale.title %></h5>
    
        <p class="card-text"><%- ihale.description %></p>                    
                    <hr>
                    <div class="d-flex justify-content-between align-items-center mb-2">
    
                        <span class="fw-bold text-secondary">
                            <%= ihale.en_yuksek_teklif ? 'Güncel Fiyat:' : 'Açılış Fiyatı:' %>
                        </span>

                        <span class="badge bg-success fs-6">
                            <%= ihale.en_yuksek_teklif ? ihale.en_yuksek_teklif : ihale.start_price %> ₺
                        </span>

</div>
                </div>

                <div class="card-footer bg-white">
                    <form action="/bid" method="post" class="d-flex gap-2">
                        <input type="hidden" name="tender_id" value="<%= ihale.tender_id %>">
                        
                        <input type="number" name="amount" class="form-control form-control-sm" placeholder="Teklifiniz" required min="<%= ihale.start_price %>">
                        
                        <button type="submit" class="btn btn-sm btn-outline-primary">Ver</button>
                    </form>
                    <div class="text-end mt-2">
                        <small class="text-muted" style="font-size: 0.75rem;">Bitiş: <%= ihale.end_date %></small>
                    </div>
                </div>
                </div>
        </div>
    <% }) %>
</div>

    
</body>
</html>