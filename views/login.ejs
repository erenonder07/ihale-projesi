<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>Giriş Yap / Kayıt Ol</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        
        .navbar {
            background: rgba(33, 37, 41, 0.95) !important;
            backdrop-filter: blur(10px);
        }

        .auth-card {
            border: none;
            border-radius: 20px;
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .auth-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0,0,0,0.1) !important;
        }

        .form-control {
            border-radius: 0 10px 10px 0;
            padding: 12px;
            border: 1px solid #dee2e6;
            border-left: none;
        }
        
        .form-control:focus {
            box-shadow: none;
            border-color: #86b7fe;
        }

        .input-group-text {
            border-radius: 10px 0 0 10px;
            background-color: white;
            border: 1px solid #dee2e6;
            border-right: none;
            color: #6c757d;
        }

        .form-control:focus + .input-group-text, 
        .form-control:focus ~ .input-group-text { 
            /* Bu kısım CSS ile zor, JS gerekebilir ama görsel olarak yeterli */
        }

        .btn-auth {
            border-radius: 10px;
            padding: 12px;
            font-weight: 600;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
        }

        .btn-login {
            background: linear-gradient(to right, #198754, #157347);
            border: none;
            color: white;
        }
        .btn-login:hover {
            background: linear-gradient(to right, #157347, #198754);
            transform: scale(1.02);
            color: white;
        }

        .btn-register {
            background: linear-gradient(to right, #0d6efd, #0b5ed7);
            border: none;
            color: white;
        }
        .btn-register:hover {
            background: linear-gradient(to right, #0b5ed7, #0d6efd);
            transform: scale(1.02);
            color: white;
        }

        .section-title {
            position: relative;
            display: inline-block;
            margin-bottom: 30px;
        }
        
        .section-title::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 50px;
            height: 3px;
            border-radius: 2px;
        }
        
        .title-login::after { background-color: #198754; }
        .title-register::after { background-color: #0d6efd; }

    </style>
</head>
<body>
  
  <nav class="navbar navbar-dark mb-5 shadow-sm">
      <div class="container">
          <a class="navbar-brand fw-bold" href="/">
               İhale<span class="text-primary">Paneli</span>
          </a>
          <a href="/" class="btn btn-sm btn-outline-light">
              <i class="fas fa-home"></i> Ana Sayfa
          </a>
      </div>
  </nav>

  <main class="container mb-5">
    <div class="row justify-content-center g-5">
        
        <div class="col-md-5">
            <div class="card auth-card shadow-lg p-4 h-100">
                <div class="card-body">
                    <div class="text-center">
                        <div class="section-title title-login">
                            <h3 class="fw-bold text-dark">Giriş Yap</h3>
                        </div>
                        <p class="text-muted small">Hesabınıza erişmek için bilgilerinizi girin.</p>
                    </div>

                    <form action="/login" method="post" class="mt-4">
                        
                        <label class="form-label text-secondary small fw-bold">E-posta Adresi</label>
                        <div class="input-group mb-3">
                            <span class="input-group-text"><i class="fas fa-envelope"></i></span>
                            <input type="email" class="form-control" name="email" placeholder="ornek@email.com" required>
                        </div>
                        
                        <label class="form-label text-secondary small fw-bold">Şifre</label>
                        <div class="input-group mb-4">
                            <span class="input-group-text"><i class="fas fa-lock"></i></span>
                            <input type="password" class="form-control" name="password" placeholder="******" required>
                        </div>
                        
                        <button type="submit" class="btn btn-login btn-auth w-100 shadow">
                            <i class="fas fa-sign-in-alt me-2"></i> Güvenli Giriş
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card auth-card shadow-lg p-4 h-100">
                <div class="card-body">
                    <div class="text-center">
                        <div class="section-title title-register">
                            <h3 class="fw-bold text-dark">Yeni Hesap</h3>
                        </div>
                        <p class="text-muted small">Hemen aramıza katılın ve ihalelere başlayın.</p>
                    </div>

                    <form action="/register" method="post" class="mt-4">
                        
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label class="form-label text-secondary small fw-bold">Ad Soyad</label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fas fa-user"></i></span>
                                    <input type="text" class="form-control" name="full_name" placeholder="Adınız" required>
                                </div>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label class="form-label text-secondary small fw-bold">Telefon</label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fas fa-phone"></i></span>
                                    <input type="text" class="form-control" name="phone" placeholder="5XX...">
                                </div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label text-secondary small fw-bold">E-posta</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-at"></i></span>
                                <input type="email" class="form-control" name="email" placeholder="ornek@email.com" required>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label text-secondary small fw-bold">TC Kimlik (Opsiyonel)</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-id-card"></i></span>
                                <input type="text" class="form-control" name="tckn" maxlength="11" placeholder="11 Hane">
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label text-secondary small fw-bold">Şifre Belirle</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-key"></i></span>
                                <input type="password" class="form-control" name="password" required minlength="6" placeholder="En az 6 karakter">
                            </div>
                        </div>

                        <button type="submit" class="btn btn-register btn-auth w-100 shadow">
                            <i class="fas fa-user-plus me-2"></i> Ücretsiz Kayıt Ol
                        </button>
                    </form>
                </div>
            </div>
        </div>

    </div>
  </main>
</body>
</html>