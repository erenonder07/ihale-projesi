<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>İlanı Düzenle</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;700&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root {
            /* YENİ MODERN RENK PALETİ */
            --primary-gradient: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
            --primary-solid: #4f46e5;
            --bg-color: #f3f4f6;
            --card-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 8px 10px -6px rgba(0, 0, 0, 0.1);
            --input-bg: #f9fafb;
            --input-border: #e5e7eb;
        }

        body { 
            font-family: 'Plus Jakarta Sans', sans-serif; 
            background-color: var(--bg-color); 
            color: #1f2937;
        }

        /* NAVBAR TASARIMI */
        .navbar {
            background: white;
            box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
        }
        .navbar-brand { 
            font-family: 'Outfit', sans-serif; 
            display: flex; 
            align-items: center; 
            gap: 12px; 
            text-decoration: none; 
        }
        .brand-icon-box { 
            width: 40px; 
            height: 40px; 
            background: var(--primary-gradient); 
            color: white; 
            border-radius: 10px; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            font-size: 1.1rem;
            box-shadow: 0 4px 6px -1px rgba(79, 70, 229, 0.3);
        }
        .brand-text-main { font-weight: 800; font-size: 1.25rem; color: #111827; letter-spacing: -0.5px; }

        /* KART TASARIMI */
        .edit-card { 
            border: none; 
            border-radius: 20px; 
            box-shadow: var(--card-shadow); 
            background: white; 
            overflow: hidden;
            position: relative;
        }
        /* Kartın tepesine ince bir renk çizgisi */
        .edit-card::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0;
            height: 6px;
            background: var(--primary-gradient);
        }

        /* FORM ELEMANLARI */
        .form-label {
            font-weight: 600;
            color: #374151;
            font-size: 0.95rem;
            margin-bottom: 8px;
        }
        .form-control {
            background-color: var(--input-bg);
            border: 1px solid var(--input-border);
            border-radius: 12px;
            padding: 12px 15px;
            font-size: 0.95rem;
            transition: all 0.2s;
        }
        .form-control:focus {
            background-color: white;
            border-color: var(--primary-solid);
            box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.1);
        }

        /* CKEDITOR ÖZELLEŞTİRME */
        .ck-editor__editable_inline {
            min-height: 300px;
            border-bottom-left-radius: 12px !important;
            border-bottom-right-radius: 12px !important;
            border-color: var(--input-border) !important;
            background-color: var(--input-bg) !important;
        }
        .ck.ck-editor__main>.ck-editor__editable:not(.ck-focused) {
            border-color: var(--input-border) !important;
        }
        .ck-toolbar {
            border-top-left-radius: 12px !important;
            border-top-right-radius: 12px !important;
            border-color: var(--input-border) !important;
            background-color: #fff !important;
        }

        /* BUTONLAR */
        .btn-primary-custom {
            background: var(--primary-gradient);
            border: none;
            color: white;
            padding: 12px 24px;
            border-radius: 12px;
            font-weight: 600;
            transition: all 0.3s;
            box-shadow: 0 4px 6px -1px rgba(79, 70, 229, 0.4);
        }
        .btn-primary-custom:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 15px -3px rgba(79, 70, 229, 0.5);
            color: white;
        }
        .btn-secondary-custom {
            background: white;
            border: 1px solid #e5e7eb;
            color: #4b5563;
            padding: 12px 24px;
            border-radius: 12px;
            font-weight: 600;
            transition: all 0.2s;
        }
        .btn-secondary-custom:hover {
            background: #f9fafb;
            color: #111827;
            border-color: #d1d5db;
        }

        /* İKON KUTUSU (BAŞLIKTAKİ) */
        .header-icon {
            width: 64px; height: 64px;
            background: #e0e7ff;
            color: var(--primary-solid);
            border-radius: 20px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg fixed-top py-3">
        <div class="container">
            <a class="navbar-brand" href="/dashboard">
                <div class="brand-icon-box"><i class="fas fa-gavel"></i></div>
                <span class="brand-text-main">İHALE SİSTEMİ</span>
            </a>
            <a href="/my-tenders" class="btn btn-secondary-custom btn-sm">
                <i class="fas fa-arrow-left me-2"></i> Vazgeç
            </a>
        </div>
    </nav>

    <div class="container py-5 mt-5">
        <div class="row justify-content-center">
            <div class="col-lg-9 col-xl-8">
                
                <div class="edit-card p-4 p-md-5 mt-4">
                    <div class="text-center mb-5">
                        <div class="header-icon">
                            <i class="fas fa-magic fa-2x"></i>
                        </div>
                        <h3 class="fw-bold text-dark">İlan Detaylarını Düzenle</h3>
                        <p class="text-muted">Aşağıdaki formu kullanarak ilanınızın tüm detaylarını güncelleyebilirsiniz.</p>
                    </div>

                    <form action="/duzenle/<%= tender.tender_id %>" method="post">
                        
                        <div class="mb-4">
                            <label class="form-label">
                                <i class="fas fa-heading me-1 text-primary opacity-75"></i> İlan Başlığı
                            </label>
                            <input type="text" name="title" class="form-control" value="<%= tender.title %>" placeholder="Örn: 2023 Model Araç..." required>
                        </div>

                        <div class="mb-4">
                            <label class="form-label">
                                <i class="fas fa-align-left me-1 text-primary opacity-75"></i> Detaylı Açıklama
                            </label>
                            <textarea id="editor" name="description"><%= tender.description %></textarea>
                            <div class="form-text ms-1 mt-1"><i class="fas fa-info-circle small"></i> İlanınızın daha dikkat çekici olması için detaylı bilgi girin.</div>
                        </div>

                        <div class="row g-4 mb-5">
                            <div class="col-md-6">
                                <label class="form-label">
                                    <i class="fas fa-tag me-1 text-primary opacity-75"></i> Başlangıç Fiyatı (₺)
                                </label>
                                <div class="input-group">
                                    <input type="number" name="start_price" class="form-control" value="<%= tender.start_price %>" required>
                                    <span class="input-group-text bg-white text-muted border-start-0" style="border-top-right-radius: 12px; border-bottom-right-radius: 12px;">₺</span>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">
                                    <i class="far fa-calendar-alt me-1 text-primary opacity-75"></i> Bitiş Tarihi
                                </label>
                                <% 
                                    let date = new Date(tender.end_date);
                                    date.setMinutes(date.getMinutes() - date.getTimezoneOffset());
                                    let dateStr = date.toISOString().slice(0, 16); 
                                %>
                                <input type="datetime-local" name="end_date" class="form-control" value="<%= dateStr %>" required>
                            </div>
                        </div>

                        <div class="d-flex gap-3">
                            <button type="button" onclick="window.location.href='/my-tenders'" class="btn btn-secondary-custom w-100">
                                İptal Et
                            </button>
                            <button type="submit" class="btn btn-primary-custom w-100">
                                <i class="fas fa-check-circle me-2"></i> Değişiklikleri Kaydet
                            </button>
                        </div>
                    </form>

                </div>
                
                <div class="text-center mt-4 text-muted small opacity-75">
                    &copy; 2025 İhale Sistemi. Tüm hakları saklıdır.
                </div>

            </div>
        </div>
    </div>

    <script src="https://cdn.ckeditor.com/ckeditor5/35.0.1/classic/ckeditor.js"></script>
    <script>
        ClassicEditor
            .create( document.querySelector( '#editor' ), {
                toolbar: [ 'heading', '|', 'bold', 'italic', 'link', 'bulletedList', 'numberedList', 'blockQuote' ],
                placeholder: 'Ürününüzün özelliklerini buraya yazın...'
            } )
            .catch( error => {
                console.error( error );
            } );
    </script>

</body>
</html>