<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>İlanı Düzenle</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;500;700;800&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f8f9fa; }
        .edit-card { border: none; border-radius: 16px; box-shadow: 0 4px 20px rgba(0,0,0,0.05); background: white; }
        
        /* CKEditor Yüksekliği (Hocanın notlarına uygun) */
        .ck-editor__editable_inline {
            min-height: 300px;
        }
    </style>
</head>
<body class="bg-light">

    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                
                <div class="edit-card p-4 p-md-5">
                    <div class="text-center mb-4">
                        <h3 class="fw-bold">✏️ İlanı Düzenle</h3>
                        <p class="text-muted">İlan detaylarını zengin metin editörü ile düzenleyebilirsiniz.</p>
                    </div>

                    <form action="/duzenle/<%= tender.tender_id %>" method="post">
                        
                        <div class="mb-3">
                            <label class="form-label fw-bold">İlan Başlığı</label>
                            <input type="text" name="title" class="form-control" value="<%= tender.title %>" required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Açıklama</label>
                            <textarea id="editor" name="description"><%= tender.description %></textarea>
                        </div>

                        <div class="row g-3 mb-4">
                            <div class="col-md-6">
                                <label class="form-label fw-bold">Fiyat (₺)</label>
                                <input type="number" name="start_price" class="form-control" value="<%= tender.start_price %>" required>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-bold">Bitiş Tarihi</label>
                                <% 
                                    let date = new Date(tender.end_date);
                                    // Tarih formatını inputa uyduruyoruz (YYYY-MM-DDTHH:MM)
                                    // Türkiye saati farkını (UTC+3) hesaba katarak düzeltme
                                    date.setMinutes(date.getMinutes() - date.getTimezoneOffset());
                                    let dateStr = date.toISOString().slice(0, 16); 
                                %>
                                <input type="datetime-local" name="end_date" class="form-control" value="<%= dateStr %>" required>
                            </div>
                        </div>

                        <div class="d-flex gap-2">
                            <a href="/my-tenders" class="btn btn-secondary w-50">İptal</a>
                            <button type="submit" class="btn btn-primary w-50 fw-bold">Kaydet</button>
                        </div>
                    </form>

                </div>

            </div>
        </div>
    </div>

    <script src="https://cdn.ckeditor.com/ckeditor5/35.0.1/classic/ckeditor.js"></script>
    <script>
        ClassicEditor
            .create( document.querySelector( '#editor' ), {
                // İstenirse buraya toolbar ayarları eklenebilir
                // removePlugins: [ 'Heading' ], // Örnek: Başlıkları kaldır
            } )
            .catch( error => {
                console.error( error );
            } );
    </script>

</body>
</html>